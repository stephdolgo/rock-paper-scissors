function drawMap() {
    //divide by height and width to make a grid
}

function ship() {
    //add triangle for ship
    //note: shapes should follow the lines of the grid rather than be centered like chess pieces
    //when ship moves, fuel goes down
}

function asteroids() {
    //add obstacles that move with duration down map infinitely
}

function collision() {
    //if obstacle collides with ship, cause damage to health bar
}